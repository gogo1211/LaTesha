# LaTesha Test Project (Backend)

Simple Ruby on Rails REST api

## Scripts

In the project directory, you can run:

### `rails server`

Runs the server in the local mode.<br>
Open [http://localhost:3001](http://localhost:3001) to view it in the browser.
